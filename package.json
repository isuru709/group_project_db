{
  "name": "group_project_db",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix backend run build && npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm --prefix backend run start"
  },
  "engines": {
    "node": ">=18"
  }
}